"use client";

// Renomme la fonction pour éviter les conflits avec la propriété globale Error
function CustomErrorPage() {
  return <div>Oups, erreur serveur</div>;
}

export default CustomErrorPage;
